# Basic usage example

A simple workflow example demonstrating core `workflow-graph` concepts.

## Purpose

This example shows how to:
- Create a simple workflow graph
- Define nodes and edges
- Handle state transitions
- Use callbacks and error handling

## Usage

```bash
pip install -r requirements.txt
python run_basic_usage.py
```